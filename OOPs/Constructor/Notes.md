
1. constructor is a block which used to initalize value to variable
2. we have two types of construtor 1. default construct and 2. parameter constructor
3. constructor defined using constructor keyword
4. define a constructor without parameter is called as default constructor
5. define a constrtor with parameter is callled as parameter constructor
6. when ever object created constructor gets exectued
7. If class doesnot have any constructor ts compiler creates default constructor

note:
=====

Inside class we can define only one constrctor(can be default constructor or parameter construtor)


class Car

brand
model
year
